function reverseStr(str) {
    // 类型检测
    if(typeof str !== 'string') {
        return 
    }
    // 数组有reverse 方法
    // 字符数组
    // 反转的字符数组
    // 变成字符串
    return str.split('').reverse().join('')
}